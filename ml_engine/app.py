from flask import Flask, request, jsonify\nfrom flask_cors import CORS\nimport logging\nimport os\nfrom datetime import datetime\nimport json\n\napp = Flask(__name__)\nCORS(app)\n\n# Logging configuration\nlogging.basicConfig(level=logging.INFO)\nlogger = logging.getLogger(__name__)\n\nclass SafetyScorer:\n    \"\"\"\n    AI-powered safety scoring engine\n    Evaluates 6 factors in real-time\n    \"\"\"\n\n    def calculate_location_risk(self, latitude: float, longitude: float,\n                               historical_incidents: int) -> float:\n        \"\"\"\n        Calculate location-based risk (0-100)\n        \"\"\"\n        base_risk = abs(latitude % 10) * 2 + abs(longitude % 10) * 1.5\n        incident_multiplier = 1 + (historical_incidents * 0.05)\n        location_risk = min(100, base_risk * incident_multiplier)\n        return location_risk\n\n    def calculate_time_risk(self, hour: int = None, day_of_week: int = None) -> float:\n        \"\"\"\n        Calculate time-based risk (0-100)\n        \"\"\"\n        if hour is None:\n            hour = datetime.now().hour\n        if day_of_week is None:\n            day_of_week = datetime.now().weekday()\n            \n        time_risk = 0\n        \n        # Night time risk (9 PM - 6 AM)\n        if hour >= 21 or hour < 6:\n            time_risk += 40\n        # Early morning (6 AM - 8 AM)\n        elif 6 <= hour < 8:\n            time_risk += 20\n        # Late evening (8 PM - 9 PM)\n        elif 20 <= hour < 21:\n            time_risk += 15\n        \n        # Weekend increased risk\n        if day_of_week >= 5:  # Saturday/Sunday\n            time_risk += 10\n        \n        return min(100, time_risk)\n\n    def calculate_crowd_risk(self, crowd_density: int) -> float:\n        \"\"\"\n        Calculate crowd-based risk (0-100)\n        \"\"\"\n        if crowd_density < 10:\n            return 85  # Very isolated\n        elif crowd_density < 30:\n            return 55  # Somewhat isolated\n        elif crowd_density <= 70:\n            return 25  # Good crowd\n        else:\n            return 35  # Too crowded\n\n    def calculate_lighting_risk(self, lighting: str) -> float:\n        \"\"\"\n        Calculate lighting-based risk (0-100)\n        \"\"\"\n        lighting_scores = {\n            'bright': 10,\n            'moderate': 35,\n            'dim': 65,\n            'dark': 95\n        }\n        return lighting_scores.get(lighting, 50)\n\n    def calculate_historical_risk(self, incidents: int) -> float:\n        \"\"\"\n        Calculate historical incident risk (0-100)\n        \"\"\"\n        if incidents == 0:\n            return 15\n        elif incidents <= 2:\n            return 30\n        elif incidents <= 5:\n            return 50\n        elif incidents <= 10:\n            return 70\n        else:\n            return 90\n\n    def calculate_driver_risk(self, driver_rating: float = 4.5, violations: int = 0) -> float:\n        \"\"\"\n        Calculate driver behavior risk (0-100)\n        \"\"\"\n        rating_risk = max(0, (5 - driver_rating) * 15)  # 0-5 stars to risk\n        violation_risk = min(violations * 8, 40)  # Cap at 40\n        return min(100, rating_risk + violation_risk)\n\n    def predict_safety_score(self, input_data: dict) -> dict:\n        \"\"\"\n        Main prediction function\n        \"\"\"\n        # Extract features\n        location_risk = self.calculate_location_risk(\n            input_data.get('latitude', 28.7),\n            input_data.get('longitude', 77.1),\n            input_data.get('historicalIncidents', 0)\n        )\n        \n        time_risk = self.calculate_time_risk(\n            input_data.get('hour'),\n            input_data.get('day_of_week')\n        )\n        \n        crowd_risk = self.calculate_crowd_risk(input_data.get('crowdDensity', 50))\n        lighting_risk = self.calculate_lighting_risk(input_data.get('lighting', 'moderate'))\n        historical_risk = self.calculate_historical_risk(input_data.get('historicalIncidents', 0))\n        driver_risk = self.calculate_driver_risk(\n            input_data.get('driverRating', 4.5),\n            input_data.get('violations', 0)\n        )\n        \n        # Weighted calculation\n        weights = [0.25, 0.20, 0.20, 0.15, 0.15, 0.05]\n        risks = [location_risk, time_risk, crowd_risk, lighting_risk, historical_risk, driver_risk]\n        \n        weighted_risk = sum(w * r for w, r in zip(weights, risks))\n        safety_score = 100 - weighted_risk\n        safety_score = max(0, min(100, safety_score))\n        \n        return {\n            'score': round(safety_score, 2),\n            'riskLevel': self._get_risk_level(safety_score),\n            'factors': {\n                'locationRisk': round(location_risk, 2),\n                'timeRisk': round(time_risk, 2),\n                'crowdRisk': round(crowd_risk, 2),\n                'lightingRisk': round(lighting_risk, 2),\n                'historicalRisk': round(historical_risk, 2),\n                'driverRisk': round(driver_risk, 2)\n            },\n            'recommendations': self._generate_recommendations(safety_score, input_data),\n            'timestamp': datetime.now().isoformat()\n        }\n\n    def _get_risk_level(self, score: float) -> str:\n        if score >= 80:\n            return 'safe'\n        elif score >= 60:\n            return 'moderate'\n        elif score >= 40:\n            return 'risky'\n        else:\n            return 'critical'\n\n    def _generate_recommendations(self, score: float, input_data: dict) -> list:\n        \"\"\"\n        Generate safety recommendations\n        \"\"\"\n        recommendations = []\n        \n        if input_data.get('lighting') in ['dark', 'dim']:\n            recommendations.append('⚠️ Low lighting - stay alert')\n        \n        if input_data.get('crowdDensity', 50) < 30:\n            recommendations.append('👥 Few people around - avoid isolated areas')\n        \n        if score < 60:\n            recommendations.append('📍 Consider alternative route')\n        \n        if score >= 80:\n            recommendations.append('✅ Area is generally safe')\n        \n        if input_data.get('driverRating', 5) < 3.5:\n            recommendations.append('⭐ Driver rating below average')\n        \n        return recommendations if recommendations else ['✨ Route looks safe']\n\n# Initialize scorer\nsafety_scorer = SafetyScorer()\n\n@app.route('/health', methods=['GET'])\ndef health():\n    \"\"\"Health check endpoint\"\"\"\n    return jsonify({\n        'status': 'healthy',\n        'timestamp': datetime.now().isoformat(),\n        'service': 'Sakhi-Safe ML Engine'\n    }), 200\n\n@app.route('/predict', methods=['POST'])\ndef predict_safety_score():\n    \"\"\"\n    Predict safety score for given input\n    \"\"\"\n    try:\n        data = request.json\n        \n        # Validate required fields\n        required_fields = ['latitude', 'longitude', 'crowdDensity', 'lighting']\n        if not all(field in data for field in required_fields):\n            return jsonify({\n                'error': 'Missing required fields',\n                'required': required_fields\n            }), 400\n        \n        # Validate data types and ranges\n        try:\n            latitude = float(data['latitude'])\n            longitude = float(data['longitude'])\n            crowd_density = int(data['crowdDensity'])\n            \n            if not (-90 <= latitude <= 90) or not (-180 <= longitude <= 180):\n                return jsonify({'error': 'Invalid latitude/longitude range'}), 400\n            \n            if not (0 <= crowd_density <= 100):\n                return jsonify({'error': 'Crowd density must be 0-100'}), 400\n        \n        except (ValueError, TypeError) as e:\n            return jsonify({'error': f'Invalid data type: {str(e)}'}), 400\n        \n        # Calculate score\n        result = safety_scorer.predict_safety_score(data)\n        \n        return jsonify(result), 200\n    \n    except Exception as e:\n        logger.error(f'Error in predict: {str(e)}')\n        return jsonify({'error': str(e)}), 500\n\n@app.route('/analyze-blind-spots', methods=['POST'])\ndef analyze_blind_spots():\n    \"\"\"\n    Analyze blind spots along a route\n    \"\"\"\n    try:\n        data = request.json\n        coordinates = data.get('coordinates', [])\n        \n        if not coordinates or len(coordinates) < 2:\n            return jsonify({'error': 'Need at least 2 coordinates'}), 400\n        \n        # Analyze each segment\n        blind_spots = []\n        for i in range(len(coordinates) - 1):\n            current = coordinates[i]\n            \n            risk_score = safety_scorer.calculate_location_risk(\n                current['latitude'], current['longitude'], 0\n            )\n            \n            if risk_score > 60:\n                blind_spots.append({\n                    'location': current,\n                    'riskScore': round(risk_score, 2),\n                    'recommendation': '⚠️ High-risk segment detected'\n                })\n        \n        return jsonify({\n            'blindSpots': blind_spots,\n            'totalSegmentsAnalyzed': len(coordinates) - 1,\n            'criticalSpots': len(blind_spots),\n            'timestamp': datetime.now().isoformat()\n        }), 200\n    \n    except Exception as e:\n        logger.error(f'Error analyzing blind spots: {str(e)}')\n        return jsonify({'error': str(e)}), 500\n\n@app.route('/heatmap', methods=['GET'])\ndef get_heatmap():\n    \"\"\"\n    Get safety heatmap for a geographic area\n    \"\"\"\n    try:\n        north = float(request.args.get('north', 28.8))\n        south = float(request.args.get('south', 28.6))\n        east = float(request.args.get('east', 77.2))\n        west = float(request.args.get('west', 77.0))\n        \n        # Generate grid of safety scores\n        grid_size = 8\n        lat_step = (north - south) / grid_size\n        lng_step = (east - west) / grid_size\n        \n        heatmap_data = []\n        \n        for i in range(grid_size):\n            for j in range(grid_size):\n                lat = south + (i * lat_step)\n                lng = west + (j * lng_step)\n                \n                risk = safety_scorer.calculate_location_risk(lat, lng, 0)\n                safety_score = 100 - risk\n                \n                heatmap_data.append({\n                    'latitude': round(lat, 4),\n                    'longitude': round(lng, 4),\n                    'safetyScore': round(safety_score, 2),\n                    'riskLevel': safety_scorer._get_risk_level(safety_score)\n                })\n        \n        return jsonify({\n            'data': heatmap_data,\n            'bounds': {\n                'north': north,\n                'south': south,\n                'east': east,\n                'west': west\n            },\n            'gridSize': grid_size,\n            'timestamp': datetime.now().isoformat()\n        }), 200\n    \n    except Exception as e:\n        logger.error(f'Error generating heatmap: {str(e)}')\n        return jsonify({'error': str(e)}), 500\n\n@app.route('/test', methods=['GET'])\ndef test():\n    \"\"\"\n    Test endpoint with sample data\n    \"\"\"\n    sample_input = {\n        'latitude': 28.7041,\n        'longitude': 77.1025,\n        'crowdDensity': 45,\n        'lighting': 'moderate',\n        'driverRating': 4.2,\n        'historicalIncidents': 2\n    }\n    \n    result = safety_scorer.predict_safety_score(sample_input)\n    return jsonify({\n        'message': 'Test prediction successful',\n        'input': sample_input,\n        'output': result\n    }), 200\n\nif __name__ == '__main__':\n    port = int(os.environ.get('PORT', 5000))\n    app.run(host='0.0.0.0', port=port, debug=True)\n"